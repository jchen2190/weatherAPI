<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WeatherAPI</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <span><%= time %></span>
        <h1>WeatherAPI</h1>
        <form autocomplete="off" action="/search-city" method="GET">
            <input id="userInput" type="text" name="myLocation" placeholder="location">
        </form>
        <div class="container-temp">
            <h2>
                <%= temp + tempSymbol %>
            </h2>
            <% if ( cloudCover < 20) { %>
                <h2><img src="./images/sun.png" alt="sunny icon">Sunny</h2>
            <% } else if ( cloudCover < 60) { %>
                <h2><img src="./images/cloudy-day.png" alt="cloudy-day icon">Partly Cloudy</h2>
            <% } else { %>
                <h2><img src="./images/cloud.png" alt="cloud icon">Cloudy</h2>
            <% } %>
        </div>
        <span><%= highTemp %>° / <%= lowTemp %>°</span>
        <br>
        <div class="weather-details">
            <span>Precipitation %</span>
        </div>
        <div class="weather-details">
            <span>Wind</span>
            
        </div>
        <div class="weather-details">
            <span>Humidity</span>

        </div>
        <div class="weather-details">
            <span>Dew Point</span>

        </div>
        <div class="weather-details">
            <span>Pressure</span>

        </div>
        <div class="weather-details">
            <span>Visibility</span>
            <span><%= visibility %></span>
        </div>
        <div class="weather-details">
            <span>Sunrise:</span>
            <span><%= sunrise %></span>
        </div>
        <div class="weather-details">
            <span>Sunset:</span>
            <span><%= sunset %></span>
        </div>
    </div>
</div>
</body>
</html>